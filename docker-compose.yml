version: '3.8'

services:
  stacksense-agent:
    build: .
    container_name: stacksense-agent
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
